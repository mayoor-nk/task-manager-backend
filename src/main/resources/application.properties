spring.application.name=task
server.port=${PORT:9090}

# PostgreSQL config
spring.datasource.url=${DATASOURCE_URL:jdbc:postgresql://localhost:5432/taskdb}
spring.datasource.username=${DATASOURCE_USER:postgres}
spring.datasource.password=${DATSOURCE_PASSWORD:Mayo@6364}
spring.datasource.dbcp2.driver-class-name=org.postgresql.Driver

# JPA config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

frontend.url=${FRONTEND_URL:http://localhost:3000}

# JWT secret (fallback warning value — override in Render)
jwt.secret=${JWT_SECRET:mysecretkeymysecretkeymysecretkey123}
jwt.expiration=${JWT_EXPIRATION_MS:10800000}